<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberMapper">
	<!-- namespace : 해당 mapper 파일의 고유 별칭 -->
	
	<!-- 
		* DML 문의 경우
		<insert|update|delete id="식별자" [parametetrType="전달받을 자바타입(풀클래스명)|별칭"]>
		</insert|update|delete>
		
		* SELECT문의 경우
		<select id="식별자" [parametetrType="전달받을 자바타입(풀클래스명)|별칭"] resultType="조회결과를반환하고자하는자바타입" | resultMap="조회결과를뽑아서매핑할id">
		</select>
		
		? 대신에 해당 sql문에 전달된 객체로부터 값을 꺼낼때 #{필드명 또는 변수명 또는 키값}
		
	 -->
	 
	 <!-- 
	 	* resultMap 마이바티스 핵심 기능 중 하나!
	 	  ResultSet으로부터 조회된 컬럼값을 하나하나씩 뽑아서 reset으로 내가 지정한 vo객체에 각 필드에 담는 (매핑) JDBC 코드를 줄여주는 역할 수행
	 	  
	 	  <resultMap id="식별자" type="조회결과를담아서반환하고자하는vo객체의타입(풀클래스명)|별칭">
	 	  	<result column="조회결과를 뽑고자하는 DB의 컬럼명" property="해당결과를 담고자하는 자바의 필드명"/>
	 	  	<result column="조회결과를 뽑고자하는 DB의 컬럼명" property="해당결과를 담고자하는 자바의 필드명"/>
	 	  	<result column="조회결과를 뽑고자하는 DB의 컬럼명" property="해당결과를 담고자하는 자바의 필드명"/>
	 	  	<result column="조회결과를 뽑고자하는 DB의 컬럼명" property="해당결과를 담고자하는 자바의 필드명"/>
	 	  	,...
	 	  </resultMap>
	  -->
	 <resultMap type="Member" id="memberResultSet">
	 	<result column="user_no" property="userNo"/>
	 	<result column="user_id" property="userId"/>
	 	<result column="user_pwd" property="userPwd"/>
	 	<result column="user_name" property="userName"/>
	 	<result column="email" property="email"/>
	 	<result column="birthday" property="birthday"/>
	 	<result column="gender" property="gender"/>
	 	<result column="phone" property="phone"/>
	 	<result column="address" property="address"/>
	 	<result column="enroll_date" property="enrollDate"/>
	 	<result column="modify_date" property="modifyDate"/>
	 	<result column="status" property="status"/>
	 </resultMap>
	 
	 
	 
	<select id="loginMember" resultMap="memberResultSet" parameterType="Member">
	select
	    user_no
	    , user_id
	    , user_pwd
	    , user_name
	    , email
	    , birthday
	    , gender
	    , phone
	    , address
	    , enroll_date
	    , modify_date
	    , status
    from member
    where user_id = #{userId}
     and user_pwd= #{userPwd}
     and status= 'Y'
	
	</select>


	<insert id="insertMember" parameterType="Member">
	insert 
    into member
    (
    user_no
    , user_id
    , user_pwd
    , user_name
    , email
    , birthday
    , gender
    , phone
    , address
    )
    values
    (
    seq_uno.nextval
    , #{userId}
    , #{userPwd}
    , #{userName}
    , #{email}
    , #{birthday}
    , #{gender}
    , #{phone}
    , #{address}
    )
	</insert>
	
	
	
		 
	 
</mapper>